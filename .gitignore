build/
install/
log/
__pycache__/
*.pyc
.claude/
# Trailing slash would match only a real directory, so a `.sim` symlink --
# handy for pointing a git worktree at the main checkout's install -- would
# show up as untracked and could be committed by accident.
.sim
.sim/
.venv-f1tenth/
